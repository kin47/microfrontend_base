abstract class ScreenName {
  /// Supported query parameters
  /// /host?tab=<tab_name>
  ///
  /// Check the tab name defined in [HostBottomTabType]
  ///
  static const String host = 'host';
  static const String splash = 'splash';
}