package com.example.microfrontendandroid.flutter.enums

abstract class MicroFrontendFlutterEvent {
    companion object {
        const val GET_AUTH_TOKEN = "getAuthToken"
        const val UPDATE_AUTH_TOKEN = "updateAuthToken"
    }
}